# dependencias
node_modules/

# build do Next
.next/
out/
tsconfig.tsbuildinfo

# ambiente (so .env.example fica versionado)
.env
.env.local
.env.development
.env.production

# config com senhas (NUNCA versionar)
api/config.php

# uploads enviados pelo admin
uploads/*
!uploads/.htaccess
!uploads/.gitkeep

# referencia de design (mockups)
design-reference/

# material bruto (fotos/videos originais, nao usado pelo site)
public/agroslaf/

# sistema
.DS_Store
